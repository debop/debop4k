database {
  host = ""
  name = "test"

  driverClass = "org.h2.Driver"
  jdbcUrl = "jdbc:h2:mem:"${database.name}
  username = "sa"
  password = ""

  maxPoolSize = 32
  minIdleSize = 2

  // testQuery = "SELECT 1"

  # PostgreSQL Settings
  preparedStatementCacheQueries = 1024
  preparedStatementCacheSizeMiB = 64
  tcpKeepAlive = true
  charset = UTF-8
}
