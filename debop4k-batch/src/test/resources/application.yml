# debop4k.data.spring.config.DataSourceProperties
debop4k:
  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:test
    username: sa
    password:
    max-pool-size: 32
    min-idle-size: 2

logging:
    level:
        root: info
        debop4k.batch: trace


